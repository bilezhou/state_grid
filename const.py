DOMAIN = "state_grid"
PACKAGE_NAME = "custom_components.state_grid"

# 版本号你可以随便改
VERSION = "0.1.0"

# 存储版本，保持和原集成一致即可
VERSION_STORAGE = 1

# 仍然使用原来的存储 key，这样继续读写 /config/.storage/state_grid.config
STORAGE_KEY = "state_grid.config"
